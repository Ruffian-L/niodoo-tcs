[package]
name = "tcs-core-wasm"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
tcs-core = { path = "../tcs-core", default-features = false, features = ["tda_rust"] }

[features]
default = []
web = []

