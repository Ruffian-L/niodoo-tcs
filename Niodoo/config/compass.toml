# Consciousness Compass Configuration
#
# Defines thresholds for mapping PAD+Topology to strategic quadrants

# Entropy threshold for high/low split
# Values above this are considered "high entropy" (uncertain, chaotic)
# Values below are "low entropy" (stable, focused)
entropy_threshold = 1.5

# β₁ (loops) threshold
# More loops than this indicates high connectivity/exploration
beta1_threshold = 2

# β₀ (components) threshold  
# More components than this indicates fragmentation
beta0_threshold = 2

